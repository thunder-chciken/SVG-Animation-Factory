{
  "name": "svg-animation-factory",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Browser-based SVG animation studio — index an SVG, keyframe it on a timeline, export production GSAP / CSS / WordPress code.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "gsap": "^3.12.5"
  },
  "devDependencies": {
    "vite": "^8.2.1"
  }
}
